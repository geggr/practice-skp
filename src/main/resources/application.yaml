spring:
  profiles:
    active: default
  
  datasource:
    url: jdbc:postgresql://localhost:5432/skillup_payments
    username: postgres
    password: docker

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

payment:
  fake_provider:
      key: "FAKE_KEY"

